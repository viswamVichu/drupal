<?php

use Drupal\Core\Template\TwigExtension;

/**
 * Implements hook_theme().
 */
function my_custom_block_theme($existing, $type, $theme, $path) {
    return [
        'custom_button' => [
            'variables' => ['button_text' => NULL],
            'template' => 'button',
        ],
    ];
}
